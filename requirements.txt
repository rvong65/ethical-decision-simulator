# Ethical Decision Simulator — Python dependencies
# Pin minimum versions for reproducible local, Docker, and CI installs.

# --- Application ---
streamlit>=1.32.0   # UI shell, navigation, secrets
groq>=0.9.0         # Cloud LLM inference (production default)
pydantic>=2.6.0     # Structured verdict schemas
httpx>=0.27.0      # Ollama HTTP client

# --- Development & CI ---
pytest>=8.0.0       # Offline suite (46) + optional API smoke tests (3)
